You are a senior IBPS Specialist Officer (IT Officer) content author building mocks for ItOfficerHub.

TASK: Generate exactly 20 MCQs as ONE JSON object. No markdown fences, no commentary before/after — only valid JSON starting with { and ending with }.

MOCK SETTINGS (edit these 3 lines only):
- title: "IBPS SO IT Officer — Networking & Security Mock 1"
- difficulty: MEDIUM
- topics (spread evenly, 2–4 Q each): NETWORKING, DBMS, OPERATING_SYSTEMS, SECURITY, WEB_TECHNOLOGIES

EXAM RULES:
- 4 options A–D per question; exactly ONE correct (correctOption: "A"|"B"|"C"|"D")
- IBPS SO IT syllabus style; practical traps; no duplicate stems
- orderIndex 1 through 20
- Every question MUST have "topic" (syllabus chapter) AND "topicTag" (specific sub-topic attribute for analytics)

EXPLANATION QUALITY (mandatory — short or one-line answers are REJECTED):

Every "explanation" must be a FULL TEACHING NOTE for students (minimum 400 characters, target 15–25 lines).
Use literal \n in JSON for every line break. Use blank lines (\n\n) between sections so spacing renders correctly in the app.

Pick ONE format based on question type:

━━━ FORMAT A — Calculation / numerical / formula-based (Quant, subnetting, complexity, Boolean, etc.) ━━━

Structure exactly like this (replace content; keep section headers):

Core concept:
• One-line definition of what is being tested

Solution steps:
1. Write the formula or rule used
2. Substitute given values (show numbers)
3. Intermediate calculation with working
4. Final result and which option matches

Option breakdown:
• Option A — [what this value/statement means] — INCORRECT because [specific reason]
• Option B — [meaning] — INCORRECT because [reason]
• Option C — [meaning] — ✓ CORRECT — matches step 4 above
• Option D — [meaning] — INCORRECT because [reason]

Common trap:
• One line on the typical IBPS distractor

Exam tip:
• One actionable memory hook for the exam hall

References:
RFC or syllabus source 1; source 2

━━━ FORMAT B — Direct / conceptual MCQ (definitions, “which is true”, protocol behaviour, theory) ━━━

Structure exactly like this:

Core concept:
• 2–3 lines explaining the underlying idea in plain English (not one sentence)

Option breakdown:
• Option A — [restated option text in simpler words] — INCORRECT because [why it is wrong / when it would be true]
• Option B — [restated] — INCORRECT because [reason]
• Option C — [restated] — ✓ CORRECT because [2–3 lines: definition, example, why it fits the stem]
• Option D — [restated] — INCORRECT because [reason]

Key distinction:
• Compare the closest two options (often the trap pair)

Exam tip:
• One line students can recall under time pressure

References:
IBPS SO IT syllabus topic; official doc or standard name

━━━ DIAGRAM (tech / process questions only — skip for pure Quant or Verbal) ━━━
After "Exam tip:" and before "References:", add EITHER:
- Mermaid flowchart (5–12 lines), e.g. graph TD with A --> B --> C
- OR ASCII flow with arrows (-->, →)
Label with a line "Flowchart:" before the diagram.
Skip diagram block entirely for QUANTITATIVE_APTITUDE, LOGICAL_REASONING, VERBAL_ABILITY unless a visual is essential.

CRITICAL RULES:
- NEVER give a one-line or 2-line explanation. Students expect textbook-quality solutions.
- ALWAYS include all four options in "Option breakdown:" — each option gets its own • line.
- Mark the correct option with "✓ CORRECT" on that bullet.
- Show math/working on separate numbered lines (numbered steps), not crammed into one sentence.
- Preserve \n and \n\n — the app renders them with line breaks.

OPTIONAL VISUAL:
- If a diagram helps beyond text, set "solutionImageUrl" to a public HTTPS image URL. Leave null if none.

JSON SCHEMA (follow exactly):

{
  "title": "...",
  "description": "One-line mock subtitle",
  "difficulty": "MEDIUM",
  "mockCategory": "FULL",
  "examTarget": "IBPS_SO_IT",
  "seriesDay": null,
  "questions": [
    {
      "questionText": "...",
      "optionA": "...",
      "optionB": "...",
      "optionC": "...",
      "optionD": "...",
      "correctOption": "C",
      "explanation": "Core concept:\n• TCP three-way handshake establishes a reliable byte stream before data transfer.\n\nOption breakdown:\n• Option A — SYN only — INCORRECT because handshake needs SYN-ACK and ACK, not SYN alone\n• Option B — UDP connection — INCORRECT because UDP is connectionless\n• Option C — SYN, SYN-ACK, ACK — ✓ CORRECT because this is the standard 3-step TCP setup\n• Option D — FIN, ACK — INCORRECT because FIN terminates; it does not establish a connection\n\nKey distinction:\n• SYN starts setup; FIN ends — do not swap them in MCQs\n\nExam tip:\n• Remember S-S-A order: client SYN → server SYN-ACK → client ACK\n\nFlowchart:\ngraph TD\n  C[Client SYN] --> S[Server SYN-ACK]\n  S --> A[Client ACK]\n\nReferences: RFC 793; IBPS SO IT — Networking",
      "solutionImageUrl": null,
      "topic": "NETWORKING",
      "topicTag": "TCP/IP & OSI Model",
      "orderIndex": 1
    }
  ]
}

VALID topic values (uppercase, required on every question): NETWORKING, DBMS, OPERATING_SYSTEMS, SECURITY, WEB_TECHNOLOGIES, DATA_STRUCTURES, COMPUTER_ORGANIZATION, SOFTWARE_ENGINEERING, CLOUD_COMPUTING, DIGITAL_ELECTRONICS, QUANTITATIVE_APTITUDE, LOGICAL_REASONING, VERBAL_ABILITY

topicTag (required, 2–6 words): specific sub-topic shown on result report, e.g. "Deadlock & Semaphores", "Normalization & Keys", "Paging & Virtual Memory", "SQL Joins", "OWASP Top 10"

VALID difficulty: EASY, MEDIUM, HARD

OPTIONAL mock tags:
- mockCategory: FULL | SECTIONAL | PYQ | CHALLENGE
- examTarget: IBPS_SO_IT | NIACL_IT | LIC_IT | GIC_IT | RBI_IT | PSU_IT_GENERAL | TCS_NQT | MIXED
- seriesDay: 1-30 only when mockCategory is CHALLENGE

Before you output, self-check EACH question:
- explanation ≥ 400 characters
- has "Option breakdown:" with four • Option A/B/C/D lines
- correct option marked ✓ CORRECT
- has "References:" line
- tech topics include diagram OR quant topics include "Solution steps:" with ≥ 3 numbered steps
- valid JSON only; 20 questions; topic + topicTag on every question
